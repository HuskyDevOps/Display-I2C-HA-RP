name: "I2C System Display"
version: "1.0.0"
slug: "i2c_display"
description: "Zeigt CPU, RAM, Temperatur und Home Assistant Status auf einem I2C 128x64 Display"
startup: services

# WICHTIG: Erzwingt lokalen Build
build: true
image: "local/i2c_display"

arch:
  - armv7
  - aarch64
  - amd64

devices:
  - /dev/i2c-1

homeassistant_api: true

options: {}
schema: {}
